services:
  ##########################
  # ZABBIX AGENT 2
  ###########################
  zabbix-agent2:
    image: zabbix/zabbix-agent2:ubuntu-7.4.3
    container_name: databus-zabbix-agent2
    hostname: databus host
    restart: unless-stopped
    privileged: true
    ports:
      - "10050:10050" # Para poder ser alcanzado por el servidor ZABBIX
    group_add:
      - "${DOCKER_GID}"
    #    networks:
    #      net0:
    #        ipv4_address: ${ZBX_AGENT2_IP}
    #user: "1001:996"
    environment:
      - TZ=America/Costa_Rica
      - ZBX_SERVER_HOST=r4.siua.ac.cr
      - ZBX_ACTIVE_ALLOW=true
      #- ZBX_HOSTNAME=${ZBX_HOSTNAME}
      - ZBX_METADATA=docker-autoreg
      - ZBX_HOSTINTERFACE=databus.siua.ac.cr
    volumes:
      - /proc:/proc
      - /sys:/sys:ro
      - /var/run/docker.sock:/var/run/docker.sock
